import { AdminPanelRequest } from './admin-panel-request';

describe('AdminPanelRequest', () => {
  it('should create an instance', () => {
    expect(new AdminPanelRequest()).toBeTruthy();
  });
});
