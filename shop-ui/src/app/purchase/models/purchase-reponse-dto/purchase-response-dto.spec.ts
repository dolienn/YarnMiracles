import { PurchaseResponseDTO } from './purchase-response-dto';

describe('PurchaseResponseDTO', () => {
  it('should create an instance', () => {
    expect(new PurchaseResponseDTO()).toBeTruthy();
  });
});
