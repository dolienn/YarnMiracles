import { PaginationAndSortParams } from './pagination-and-sort-params';

describe('PaginationAndSortParams', () => {
  it('should create an instance', () => {
    expect(new PaginationAndSortParams()).toBeTruthy();
  });
});
